---
plugin: amazon.aws.aws_ec2

regions:
  - ap-south-1

# Filters me se tag:Project hata diya hai
filters:
  instance-state-name: running
  tag:component:
    - "vmstorage"
    - "vm-app"
    - "vmalert"
    - "grafana"

hostnames:
  - ip-address
  - dns-name
  - private-ip-address

keyed_groups:
  - key: tags.component
    prefix: ""
    separator: ""

  - key: tags.extrarole
    prefix: ""
    separator: ""

groups:
  vminsert: "'vm-app' in (tags.component | default(''))"
  vmselect: "'vm-app' in (tags.component | default(''))"
  vmstorage: "'vmstorage' in (tags.component | default(''))"
  vmalert: "'vmalert' in (tags.extrarole | default('')) or 'vmalert' in (tags.component | default(''))"
  grafana: "'grafana' in (tags.component | default('')) or 'grafana' in (tags.extrarole | default(''))"
  vmagent: "true"